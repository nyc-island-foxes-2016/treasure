<div class='available-items-container'>

  <% if @item %>
    <%= render partial: "swipe_item", locals: {item: @item} %>

    <%= form_for @swipe, html: {class: 'swipe-action', id: 'swipe-left'} do |f| %>
      <%= f.hidden_field :direction, value: "L" %>
      <%= f.hidden_field :other_item_id, value: @item.id %>
      <%= f.submit "swipe left", class: "sr-only" %>
      <%= f.button class: 'btn btn-danger btn-lg', name:"commit" do %>
        <i class="fa fa-close"></i>
      <% end %>


    <% end %>


    <%= form_for @swipe, html: {class: 'swipe-action', id: 'swipe-right'} do |f| %>
      <%= f.hidden_field :direction, value: "R" %>
      <%= f.hidden_field :other_item_id, value: @item.id %>
      <%= f.submit "swipe right", class: "sr-only" %>
      <%= f.button class: 'btn btn-success btn-lg', name:"commit" do %>
        <i class="fa fa-check"></i>
      <% end %>


    <% end %>

  <% else %>

    <% if flash[:notice] %>
      <p><%= flash[:notice] %></p>
    <% end %>

  <% end %>
</div>
